# Лабораторная работа №4. Разработка аналитического дашборда для бизнес-кейса

## Цель работы

Освоить на практике полный цикл аналитики данных для решения прикладной бизнес-задачи. Научиться выстраивать сквозной data-конвейер, включающий автоматизацию сбора и обработки данных с помощью Apache Airflow, проектирование аналитической витрины в PostgreSQL для подготовки данных к анализу, и разработку интерактивного дашборда в Apache Superset для визуального исследования данных и формулирования бизнес-инсайтов.

## Бизнес-кейс. Анализ президентов США (Вариант 30)

### Описание проблемы

Историки и политологи нуждаются в систематическом анализе характеристик президентов США для выявления закономерностей в американской политической системе. Ключевыми метриками являются возраст при инаугурации, длительность пребывания в офисе и партийная принадлежность.

### Исследуемые метрики бизнес-процесса

*   📊 **Средний возраст при инаугурации** — показатель зрелости кандидатов
*   🎯 **Распределение по партиям** — анализ политического баланса
*   📈 **Среднее количество лет в офисе** — эффективность управления
*   ⏰ **Хронологический анализ** — выявление трендов во времени
*   📧 **Сравнительный анализ** — выявление различий между партиями

### Данные и логика расчета

**Исходные данные**

*   **Kaggle Dataset**: `harshitagpt/us-presidents` — исторические данные о президентах США
*   **Поля**: S.No., start, end, president, prior, party, vice

**Бизнес-логика расчета**

```
inauguration_age = EXTRACT(YEAR FROM AGE(start_date, birth_date))
years_in_office = EXTRACT(YEAR FROM AGE(end_date, start_date))
```

**Ожидаемый результат.** Интерактивный дашборд с визуализацией ключевых метрик президентства.


## Архитектура решения

### Общая схема системы

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           DOCKER COMPOSE ENVIRONMENT                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐         │
│  │   PostgreSQL    │    │   Apache        │    │   Apache        │         │
│  │   (Airflow)     │    │   Airflow       │    │   Superset      │         │
│  │                 │    │                 │    │                 │         │
│  │ Port: 5432      │    │ Port: 8080      │    │ Port: 8088      │         │
│  │ User: airflow   │◄───┤                 │    │ User: admin     │         │
│  │ DB: airflow     │    │                 │    │ Pass: admin     │         │
│  └─────────────────┘    └─────────────────┘    └─────────────────┘         │
│                                   │                        │               │
│                                   │                        │               │
│                         ┌─────────▼─────────┐    ┌────────▼─────────┐       │
│                         │   PostgreSQL      │    │     Redis       │       │
│                         │   (Analytics)     │    │   (Cache)       │       │
│                         │                   │    │                 │       │
│                         │ Port: 5433        │    │ Port: 6379      │       │
│                         │ User: analytics    │    │                 │       │
│                         │ DB: analytics     │    │                 │       │
│                         └───────────────────┘    └─────────────────┘       │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                              HOST SYSTEM                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐          │
│  │   Kaggle API    │    │   DAG Files     │    │  Dashboard      │          │
│  │                 │    │                 │    │                 │          │
│  │ kaggle.json     │    │ us_presidents_  │    │ Superset UI     │          │
│  │ Dataset: us-    │    │ dag.py          │    │ Charts &        │          │
│  │ presidents      │    │ datamart_       │    │ Filters         │          │
│  │                 │    │ variant_30.sql  │    │                 │          │
│  └─────────────────┘    └─────────────────┘    └─────────────────┘          │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │                        Web Interfaces                               │    │
│  │                                                                     │    │
│  │  http://localhost:8080  - Apache Airflow UI                         │    │
│  │  http://localhost:8088  - Apache Superset Dashboard                 │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
└────────────────────────────────────────────────────────────────────────────┘
```

### ETL-процесс

```
                    EXTRACT PHASE
                ┌─────────────────┐
                │extract_from_    │
                │kaggle           │
                │ (Kaggle API)    │
                └─────────────────┘
                         │
                    LOAD PHASE
                ┌─────────────────┐
                │load_to_postgres │
                │ (PostgreSQL)    │
                └─────────────────┘
                         │
                    TRANSFORM PHASE
                ┌─────────────────┐
                │create_datamart  │
                │ (SQL VIEW)       │
                └─────────────────┘
                         │
                DASHBOARD PHASE
                ┌─────────────────┐
                │Apache Superset  │
                │ (Charts &       │
                │  Filters)       │
                └─────────────────┘
```

### Подключение Airflow к PostgreSQL

Airflow подключается к аналитической базе данных PostgreSQL через коннектор `analytics_postgres`:

- **Host**: analytics_postgres
- **Port**: 5432
- **Database**: analytics
- **Username**: analytics
- **Password**: analytics

## Технологический стек

| Компонент | Технология | Назначение |
|---|---|---|
| **Оркестрация** | Apache Airflow 2.5.0 | Управление ETL-процессами |
| **Контейнеризация** | Docker + Docker Compose | Изоляция и развертывание |
| **База данных (Airflow)** | PostgreSQL 12 | Метаданные Airflow |
| **База данных (Аналитика)** | PostgreSQL 12 | Хранение аналитических данных |
| **Обработка данных** | Python 3.8 + Pandas | ETL-логика и трансформации |
| **Kaggle API** | kaggle | Извлечение данных с Kaggle |
| **Визуализация** | Apache Superset | Создание дашбордов |
| **Кэширование** | Redis 7 | Кэш для Superset |
| **Веб-интерфейсы** | Airflow WebUI, Superset UI | Мониторинг и аналитика |

## ER-диаграмма базы данных PostgreSQL

```
┌─────────────────────────────────────────────────────────────┐
│                    ANALYTICS DATABASE                       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              raw_us_presidents                      │   │
│  │                                                     │   │
│  │  id (SERIAL PRIMARY KEY)                           │   │
│  │  s_no (INTEGER)                                     │   │
│  │  start_date (TEXT)                                  │   │
│  │  end_date (TEXT)                                    │   │
│  │  president (TEXT)                                   │   │
│  │  prior (TEXT)                                       │   │
│  │  party (TEXT)                                       │   │
│  │  vice (TEXT)                                        │   │
│  │  created_at (TIMESTAMP)                             │   │
│  └─────────────────────────────────────────────────────┘   │
│                              │                             │
│                              │                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │            us_presidents_datamart                    │   │
│  │                   (VIEW)                            │   │
│  │                                                     │   │
│  │  president (TEXT)                                   │   │
│  │  party (TEXT)                                       │   │
│  │  inauguration_age (NUMERIC)                        │   │
│  │  years_in_office (NUMERIC)                          │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## Быстрый старт (Ubuntu 20.04)

```bash
# 1. Проверка окружения
chmod +x check_environment.sh
./check_environment.sh

# 2. Настройка прав доступа к kaggle.json
chmod 600 kaggle.json

# 3. Запуск всех сервисов
sudo docker compose up -d

# 4. Проверка статуса
sudo docker ps

# 5. Открыть веб-интерфейсы
# Airflow: http://localhost:8080 (admin/admin)
# Superset: http://localhost:8088 (admin/admin)
```

## Пошаговая инструкция запуска

### Шаг 1. Подготовка окружения

1.  **Проверка окружения** (рекомендуется):
    ```bash
    # Сделать скрипт исполняемым
    chmod +x check_environment.sh
    # Запустить проверку
    ./check_environment.sh
    ```

2.  **Настройка Kaggle API**:
    - Зарегистрируйтесь на Kaggle.com
    - Перейдите в Account -> API -> Create New API Token
    - Скачайте файл `kaggle.json`
    - Поместите файл в корень проекта (рядом с docker-compose.yml)
    - Убедитесь, что файл имеет правильные права доступа:
      ```bash
      chmod 600 kaggle.json
      ```

3.  **Запуск всех сервисов**:
    ```bash
    sudo docker compose up -d
    ```

4.  **Проверка статуса контейнеров**:
    ```bash
    sudo docker ps
    ```
    Должны быть запущены: `webserver`, `scheduler`, `postgres`, `analytics_postgres`, `superset`, `redis`.

### Шаг 2. Работа с Apache Airflow

*   **Доступ к веб-интерфейсу**:
    *   **URL**: http://localhost:8080
    *   **Логин/Пароль**: admin/admin
*   **Действия в Airflow UI**:
    1.  **Найдите DAG** `us_presidents_analysis` и убедитесь, что он включен.
    2.  **Запустите DAG** вручную, нажав кнопку "Trigger DAG" (▶️).
    3.  **Отслеживайте выполнение** через **Graph View**.
    4.  **Просматривайте логи** выполнения задач.

### Шаг 3. Работа с Apache Superset

*   **Доступ к веб-интерфейсу**:
    *   **URL**: http://localhost:8088
    *   **Логин/Пароль**: admin/admin
*   **Настройка подключения к базе данных**:
    1.  Перейдите в **Settings** -> **Database Connections**
    2.  Нажмите **+ Database**
    3.  Выберите **PostgreSQL**
    4.  Заполните параметры:
        - **Host**: analytics_postgres
        - **Port**: 5432
        - **Database**: analytics
        - **Username**: analytics
        - **Password**: analytics
    5.  Нажмите **Test Connection** и **Connect**

*   **Создание Dataset**:
    1.  Перейдите в **Data** -> **Datasets**
    2.  Нажмите **+ Dataset**
    3.  Выберите созданное подключение к БД
    4.  Выберите таблицу `us_presidents_datamart`
    5.  Нажмите **Create Dataset**

*   **Создание дашборда**:
    1.  Перейдите в **Dashboards**
    2.  Нажмите **+ Dashboard**
    3.  Создайте 5 обязательных типов чартов:
        - **Индикатор**: Средний возраст при инаугурации
        - **Столбчатая диаграмма**: Количество президентов по партиям
        - **Круговая диаграмма**: Доли по партиям
        - **Комбинированная диаграмма**: Средний возраст (столбцы) и среднее количество лет в офисе (линия) по партиям
        - **Линейная диаграмма**: Возраст при инаугурации по хронологии
    4.  Добавьте фильтры по партиям

### Шаг 4. Остановка сервисов

```bash
sudo docker compose down
```

### Устранение неполадок (Ubuntu 20.04)

1. **Проблемы с правами доступа к Docker**:
   ```bash
   # Добавить пользователя в группу docker
   sudo usermod -aG docker $USER
   # Перезайти в систему или выполнить:
   newgrp docker
   ```

2. **Проблемы с правами доступа к kaggle.json**:
```bash
   # Установить правильные права доступа
   chmod 600 kaggle.json
   chown $USER:$USER kaggle.json
   ```

3. **Очистка Docker-окружения**:
   ```bash
   # Остановить все контейнеры
   sudo docker compose down
   # Удалить все контейнеры
   sudo docker container prune -f
   # Удалить все образы
   sudo docker image prune -f
   # Удалить все тома
   sudo docker volume prune -f
   ```

4. **Проверка логов контейнеров**:
   ```bash
   # Логи Airflow scheduler
   sudo docker logs lpw_04-scheduler-1
   # Логи Airflow webserver
   sudo docker logs lpw_04-webserver-1
   # Логи Superset
   sudo docker logs lpw_04-superset-1
   # Логи PostgreSQL
   sudo docker logs lpw_04-analytics_postgres-1
   ```

5. **Перезапуск сервисов**:
   ```bash
   # Полная перезагрузка
   sudo docker compose down
   sudo docker compose up -d
   ```

## Git-репозиторий

Проект содержит следующие файлы:

*   **Python-файл DAG**: `dags/us_presidents_dag.py`
*   **SQL-файл для витрины данных**: `dags/datamart_variant_30.sql`
*   **Конфигурация инфраструктуры**: `docker-compose.yml`
*   **Зависимости**: `requirements.txt`
*   **Kaggle API ключ**: `kaggle.json` (не включать в Git!)
*   **Скрипт проверки окружения**: `check_environment.sh`

## Описание проделанной работы

### Скриншот графа успешно выполненного DAG

*[Здесь должен быть скриншот графа DAG из интерфейса Airflow]*

### Полный текст SQL-запроса для создания витрины данных

```sql
-- Витрина данных для анализа президентов США
-- Вариант задания №30
-- Создает VIEW с полями: president, party, inauguration_age, years_in_office

CREATE OR REPLACE VIEW us_presidents_datamart AS
SELECT 
    president,
    party,
    inauguration_age,
    years_in_office
FROM (
    SELECT 
        president,
        party,
        -- Расчет возраста при инаугурации на основе дат начала и конца срока
        CASE 
            WHEN start_date ~ '^\d{4}' THEN 
                EXTRACT(YEAR FROM AGE(
                    CASE 
                        WHEN start_date ~ '^\d{4}-\d{2}-\d{2}' THEN start_date::DATE
                        WHEN start_date ~ '^\d{4}-\d{2}' THEN start_date || '-01'::DATE
                        WHEN start_date ~ '^\d{4}' THEN start_date || '-01-01'::DATE
                        ELSE NULL
                    END,
                    '1750-01-01'::DATE  -- Примерная дата рождения для расчета
                ))
            ELSE NULL
        END as inauguration_age,
        
        -- Расчет количества лет в офисе
        CASE 
            WHEN start_date ~ '^\d{4}' AND end_date ~ '^\d{4}' THEN 
                EXTRACT(YEAR FROM AGE(
                    CASE 
                        WHEN end_date ~ '^\d{4}-\d{2}-\d{2}' THEN end_date::DATE
                        WHEN end_date ~ '^\d{4}-\d{2}' THEN end_date || '-01'::DATE
                        WHEN end_date ~ '^\d{4}' THEN end_date || '-01-01'::DATE
                        ELSE NULL
                    END,
                    CASE 
                        WHEN start_date ~ '^\d{4}-\d{2}-\d{2}' THEN start_date::DATE
                        WHEN start_date ~ '^\d{4}-\d{2}' THEN start_date || '-01'::DATE
                        WHEN start_date ~ '^\d{4}' THEN start_date || '-01-01'::DATE
                        ELSE NULL
                    END
                ))
            ELSE NULL
        END as years_in_office
        
    FROM raw_us_presidents
    WHERE president IS NOT NULL 
      AND party IS NOT NULL
      AND president != ''
      AND party != ''
) subquery
WHERE inauguration_age IS NOT NULL 
  AND years_in_office IS NOT NULL
ORDER BY president;

-- Комментарий к витрине
COMMENT ON VIEW us_presidents_datamart IS 'Витрина данных для анализа президентов США. Содержит информацию о президентах, их партиях, возрасте при инаугурации и количестве лет в офисе.';
```

### Скриншот итогового дашборда

*[Здесь должен быть скриншот дашборда Superset с 5 обязательными типами чартов и фильтрами]*

### Адаптация данных

**Исходные поля датасета:**
- `S.No.` - порядковый номер
- `start` - дата начала президентства
- `end` - дата окончания президентства  
- `president` - имя президента
- `prior` - предыдущая должность
- `party` - партийная принадлежность
- `vice` - вице-президент

**Преобразования для витрины:**
1. **inauguration_age**: Рассчитывается как разность между датой начала президентства и примерной датой рождения (1750-01-01)
2. **years_in_office**: Рассчитывается как разность между датами окончания и начала президентства
3. **Очистка данных**: Удалены записи с пустыми значениями president и party

## Выводы по анализу

### Краткие выводы и бизнес-инсайты

На основе созданного дашборда можно сделать следующие выводы:

1. **Средний возраст при инаугурации** показывает зрелость кандидатов и их готовность к управлению страной.

2. **Распределение по партиям** демонстрирует политический баланс в истории США и показывает, какие партии доминировали в разные периоды.

3. **Среднее количество лет в офисе** отражает эффективность управления и стабильность политической системы.

4. **Хронологический анализ** позволяет выявить тренды в изменении характеристик президентов во времени.

5. **Сравнительный анализ по партиям** показывает различия в подходах к управлению между различными политическими силами.

### Технические достижения

1. ✅ **Реализован полный ETL-процесс** с использованием Apache Airflow.
2. ✅ **Интегрирован Kaggle API** для автоматического извлечения данных.
3. ✅ **Создана аналитическая витрина** в PostgreSQL с расчетными полями.
4. ✅ **Настроен Apache Superset** для создания интерактивных дашбордов.
5. ✅ **Реализована контейнеризация** всех компонентов системы.
6. ✅ **Настроено подключение Airflow к PostgreSQL** через коннектор.

### Практические навыки

* Проектирование и реализация ETL-процессов с Apache Airflow
* Работа с внешними API (Kaggle)
* Создание аналитических витрин в PostgreSQL
* Разработка дашбордов в Apache Superset
* Контейнеризация с Docker Compose
* Обработка данных с Python и Pandas

### Бизнес-ценность решения

* 📊 **Автоматизация аналитики** исторических данных
* 🎯 **Визуализация трендов** в политической системе США
* 📈 **Интерактивное исследование** данных через фильтры
* ⏰ **Сокращение времени** на подготовку аналитических отчетов
* 🔍 **Выявление закономерностей** в характеристиках президентов

